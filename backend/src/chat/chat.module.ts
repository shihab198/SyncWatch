import { Module } from '@nestjs/common';

// Chat is handled via WebSocket in SyncGateway
// This module is a placeholder for future REST chat endpoints if needed
@Module({})
export class ChatModule {}
